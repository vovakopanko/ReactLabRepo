export enum ModalForm {
  REGISTRATION = "registration",
  AUTHORIZATION = "authorization",
}

export enum PortalNames {
  REGISTRATION = "Registration",
  AUTHORIZATION = "Authorization",
}

export enum Roles {
  ADMIN = "ADMIN",
  PLAYER = "PLAYER",
}
