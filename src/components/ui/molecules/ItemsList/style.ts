import { colors } from "@/styles/palette";
import styled from "styled-components";

export const StyleItemsList = styled.ul`
  padding: 0 20px;
`;

export const Item = styled.li`
  font-size: 18px;
  color: ${colors.WHITE};
  list-style-type: none;
`;
