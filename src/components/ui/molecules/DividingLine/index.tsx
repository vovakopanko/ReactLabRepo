import { FC } from "react";
import { LineContainer, StyleLine } from "./type";

const Line: FC = () => {
  return (
    <LineContainer>
      <StyleLine />
    </LineContainer>
  );
};

export default Line;
