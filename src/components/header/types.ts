export type MenuItem = {
  id: number;
  item: string;
  link: string;
  setSelectedProduct?: any;
  selectedProduct?: boolean;
};

export type dropDownItem = {
  id: number;
  title: string;
  link: string;
};
