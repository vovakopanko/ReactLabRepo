export type ItemComponent = {
  id?: number;
  name: string;
  componentName: JSX.Element;
};

export type Category = {
  id?: number;
  alt: string;
  title: string;
  image: string;
};

export type CardItem = {
  title: string;
};
