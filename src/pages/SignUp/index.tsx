import { PageContainerInfo } from "@/components/ui/atoms";
import { FC } from "react";

const SignUp: FC = () => {
  const pageInfo = "Soon there will be content for SignUp";
  return <PageContainerInfo pageInfo={pageInfo} />;
};

export default SignUp;
